# Video.js Local Assets

Place local Video.js assets here for offline or restricted-network environments:

- `video.min.js`
- `video-js.css`

Recommended source: https://vjs.zencdn.net/8.21.0/

The player page loads local files first, then CDN as fallback.
If both are unavailable, the app automatically falls back to native HTML5 video player.
